[project]
name = "pv-solar-api"
version = "0.1.0"
description = "Huawei inverter monitoring and discharge windows, over the inverter's local Modbus link"
requires-python = ">=3.11"
dependencies = [
    "fastapi[standard]>=0.115",
    "python-dotenv>=1.0",
    "httpx>=0.27",
    "tzdata>=2024.1",
    "firebase-admin>=6.5",
    "pillow>=12.3.0",
    "huawei-solar>=2.5.0",
    "astral>=3.2,<4",
]

[project.optional-dependencies]
test = [
    "pytest>=8.0",
    "pytest-asyncio>=0.24",
    "pytest-mock>=3.12",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
pythonpath = ["."]

[tool.uv.sources]
